<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../../partials/head'); %>
</head>
<body class="container">
    <%- include('../../partials/header'); %>
<main>
    <div class="container">
      <% for (item of test) { 
          if(item.id == id){
        %>
          <div class="wrapper" style="text-align: -webkit-center;">
            <form action="/" method="post" name="Login_Form" class="form-signin" style="width: 30%;">
                <h3 class="form-signin-heading marge">Please Update</h3>

                <input type="text" class="form-control marge" value="<%= item.mail %>" name="mail" placeholder="<%= item.mail %>" required="" autofocus="" />
                <input type="text" class="form-control marge" value="<%= item.username %>"  name="name" placeholder="Name" required=""/>
                <input type="text" class="form-control marge" value="<%= item.phone %>"  name="name" placeholder="Name" required=""/>

                <button class="btn btn-lg btn-primary btn-block marge" name="Submit" value="Register" type="Submit">Update</button>
            </form>
        </div>

        <% }} %>
    </div>
</main>
   <%- include('../../partials/footer'); %> 
</body>
</html>